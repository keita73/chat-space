
.Main__Chat__Box
  .Main__Chat__Box__User
    .Main__Chat__Box__User__Name
      = message.user.name
    .Main__Chat__Box__User__Date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .Main__Chat__Box__Message
    - if message.content.present?
      %p.Main__Chat__Box__Message__Content
        = message.content
    = image_tag message.image.url, class: 'Main__Chat__Box__Message__image' if message.image.present?